# Environment Variables for Docker Compose
# Copy this file to .env and update with your values
# DO NOT commit .env to version control

# JWT Secret (REQUIRED in production)
# Generate a strong secret: openssl rand -base64 32
JWT_SECRET=change-this-secret-key-in-production

# JWT claim metadata (override per environment if needed)
JWT_ISSUER=goReporter
JWT_AUDIENCE=goReporter-client

# Database Reset Options (Optional)
# DB_RESET=drop     # Drops all tables and recreates them
# DB_SEED=1         # Forces re-seeding of database

# PostgreSQL Configuration (if you want to override defaults)
# POSTGRES_DB=goreporter
# POSTGRES_USER=goreporter
# POSTGRES_PASSWORD=changeme

# Application Port (if you want to override default)
# APP_PORT=5000
